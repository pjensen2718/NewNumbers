

#include "BigNumber.h"

using namespace std;


ostream & operator<<(ostream &os, const BigNumber &num) {
    //todo
    return os;
}

istream & operator>>(istream &is, BigNumber &num) {
    //todo
    return is;
}